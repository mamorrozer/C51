/*------------------------------------------------------------
 * 文件：delay.h
 * 作用：提供毫秒级阻塞延时接口，供 OLED 初始化、EEPROM 写周期等待等场景使用。
 * 框架：仅声明 DelayMs(ms)；具体实现见 delay.c。
 *-----------------------------------------------------------*/
#ifndef __DELAY_H__
#define __DELAY_H__

/* 阻塞延时 ms 毫秒（受晶振频率与编译优化影响）。 */
void DelayMs(unsigned int ms);

#endif
